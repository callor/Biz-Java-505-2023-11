package com.callor.hello.arrays;

public class ArraysAB {

	public static void main(String[] args) {
		int num = 0;
		for(int i = 0; i < 5 ; i++) {
			int index = i+1;
			int numindex = 0;
		}
		
		for(int i = 0; i < 5 ;i++) {
			int index = i + 1;
			/*
			 * num 라는 변수와 index 라는 변수가 서로 합성되어
			 * num1, num2, num3, num4, num5 라는 변수가
			 * 만들어질것으로 기대했지만 안된다
			 */
			
			// numindex = (int)(Math.random() * 50) + 51;
		}
	}
}
